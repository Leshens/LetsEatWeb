import React from 'react';
import '../App.css';
import RestaurantFormController from '../controllers/RestaurantFormController';

const RestaurantForm = () => {
  return <RestaurantFormController />;
};

export default RestaurantForm;
